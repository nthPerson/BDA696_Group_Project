"""Gate / exercise-recognition models (docs/02-system-design.md §6).

Planned: ``energy.py`` (motion-energy baseline), ``rf.py`` (random forest on window features),
``cnn.py`` (Keras 1D-CNN), ``export.py`` (int8 TFLite + ``gate_model_data.cc`` for firmware).
Requires the ``train`` extra for Keras/PyTorch.
"""
