"""Webcam pose estimation and joint angles.

Planned modules (Checkpoint 3): ``landmarker.py`` (MediaPipe PoseLandmarker wrapper),
``normalize.py`` (world landmarks, torso-unit scaling, smoothing), ``angles.py`` (elbow,
shoulder, hip, knee, trunk) and ``reps.py`` (rep segmentation from the primary joint angle).
Requires the ``vision`` extra: ``uv sync --extra vision``.
"""
